1. High: Connection.createStatement(int, int) and createStatement(int, int, int) always throw, even for the only supported combination. That contradicts the documented JDBC support and the
prepareStatement(...) behavior, so clients that explicitly request TYPE_FORWARD_ONLY / CONCUR_READ_ONLY / CLOSE_CURSORS_AT_COMMIT will fail for no good reason. See README.md:163,
JParqConnection.java:279, JParqConnection.java:286, JParqConnection.java:373, JParqConnectionTest.java:30.

   Plan:
   a. In JParqConnection.createStatement(int, int) (~line 279): replace the unconditional throw with
      a parameter check — if resultSetType == TYPE_FORWARD_ONLY && resultSetConcurrency == CONCUR_READ_ONLY,
      delegate to createStatement(). Otherwise throw SQLFeatureNotSupportedException.
      This mirrors the existing prepareStatement(String, int, int) pattern at line 373.
   b. In JParqConnection.createStatement(int, int, int) (~line 286): check resultSetHoldability first —
      if it is not CLOSE_CURSORS_AT_COMMIT, throw. Otherwise delegate to
      createStatement(resultSetType, resultSetConcurrency).
      This mirrors the existing prepareStatement(String, int, int, int) pattern at line 382.
   c. Update JParqConnectionTest.createStatementWithTypeAndConcurrencyIsUnsupported (~line 30):
      change the test so it asserts success for (TYPE_FORWARD_ONLY, CONCUR_READ_ONLY) and throws
      only for unsupported combinations (e.g. TYPE_SCROLL_INSENSITIVE, CONCUR_UPDATABLE).
      Add a companion test for the three-arg overload covering both the happy path
      (TYPE_FORWARD_ONLY, CONCUR_READ_ONLY, CLOSE_CURSORS_AT_COMMIT) and the rejection path
      (e.g. HOLD_CURSORS_OVER_COMMIT).

2. High: statement lifecycle state is broken. JParqStatement.close() never marks the statement closed and isClosed() always returns false; JParqPreparedStatement.close() only clears a nested bound
   statement and its isClosed() also always returns false. That is a JDBC contract violation and will confuse pools, wrappers, and any code that relies on closed-state checks. See
   JParqStatement.java:141, JParqStatement.java:285, JParqPreparedStatement.java:506, JParqPreparedStatement.java:4234, JParqStatementTest.java:150, JParqPreparedStatementCoverageTest.java:168.

   Plan:
   a. JParqStatement: add a private boolean field `closed = false` (next to the existing fields at ~line 17).
   b. JParqStatement.close() (~line 141): after closing currentRs, set `closed = true`.
   c. JParqStatement.isClosed() (~line 285): return the `closed` field instead of hardcoded `false`.
   d. JParqPreparedStatement: add a private boolean field `closed = false` (in the field block ~line 154).
   e. JParqPreparedStatement.close() (~line 506): after closing the boundStatement, also delegate to
      `stmt.close()` (to close the wrapped JParqStatement) and set `closed = true`.
   f. JParqPreparedStatement.isClosed() (~line 4234): return `closed` instead of hardcoded `false`.
   g. Update JParqStatementTest (~line 150): the existing assertion `assertFalse(statement.isClosed())`
      on an open statement stays. Add a new assertion after `statement.close()` that verifies
      `assertTrue(statement.isClosed())`. Expand the existing testClose (~line 97) to also assert
      `assertTrue(statement.isClosed())` after close.
   h. Update JParqPreparedStatementCoverageTest (~line 168): same pattern — keep the assertFalse on
      an open statement, add assertTrue after close().

3. Medium: several Connection methods return misleading sentinel values instead of either real behavior or SQLFeatureNotSupportedException. The worst examples are isValid() always returning false on an
   open connection, the LOB/array/struct factories returning null, and unwrap() returning null. That is likely to break framework integration with hard-to-diagnose NPEs or false "dead connection"
   detection. See JParqConnection.java:462, JParqConnection.java:482, JParqConnection.java:505, JParqConnection.java:537, JParqConnectionCoverageTest.java:71.

   Plan:
   a. isValid(int timeout) (~line 482): change to `return !isClosed();`. An open JParq connection is
      always valid (it reads local Parquet files; there is no remote server to probe). This is a real
      implementation, not a stub.
   b. createClob() (~line 462): throw SQLFeatureNotSupportedException("Clob is not supported.").
   c. createBlob() (~line 467): throw SQLFeatureNotSupportedException("Blob is not supported.").
   d. createNClob() (~line 472): throw SQLFeatureNotSupportedException("NClob is not supported.").
   e. createSQLXML() (~line 477): throw SQLFeatureNotSupportedException("SQLXML is not supported.").
   f. createArrayOf(String, Object[]) (~line 505): throw SQLFeatureNotSupportedException("Array is not supported.").
   g. createStruct(String, Object[]) (~line 510): throw SQLFeatureNotSupportedException("Struct is not supported.").
   h. unwrap(Class<T>) (~line 537): if `iface.isInstance(this)` return `iface.cast(this)`, otherwise
      throw `SQLException("Cannot unwrap to " + iface.getName())`. This is the JDBC-correct behavior
      for Wrapper.unwrap().
   i. isWrapperFor(Class<?>) (near unwrap): if currently returning false, change to
      `return iface.isInstance(this)`.
   j. Update JParqConnectionCoverageTest.defaultFactoriesAndSettings (~line 71):
      - Change `assertFalse(connection.isValid(0))` to `assertTrue(connection.isValid(0))`.
      - Change the six `assertNull(connection.create…)` assertions to
        `assertThrows(SQLFeatureNotSupportedException.class, () -> connection.create…(…))`.
      - Change `assertNull(connection.unwrap(Object.class))` to
        `assertThrows(SQLException.class, () -> connection.unwrap(Object.class))`.
      - Add `assertTrue(connection.isWrapperFor(JParqConnection.class))`.
   k. Update README.md (~line 167): change "Connection validation (isValid) is not supported" to
      reflect that isValid() now works (returns true when open, false when closed).

4. Medium: version reporting is inconsistent across the build and runtime metadata. The Maven project is 1.3.0-SNAPSHOT, DatabaseMetaData reports 1.3.0 / 1.2, and Driver reports 0.1. Anything
   inspecting driver versions will get contradictory answers. See pom.xml:7, JParqDatabaseMetaData.java:417, JParqDatabaseMetaData.java:432, JParqDriver.java:62, JParqDriverTest.java:45.

   Plan:
   a. Create a small constant class `JParqVersion.java` (in se.alipsa.jparq) with:
        public static final int MAJOR = 1;
        public static final int MINOR = 3;
        public static final int PATCH = 0;
        public static final String VERSION = MAJOR + "." + MINOR + "." + PATCH;
      This becomes the single source of truth for all runtime version queries.
   b. JParqDatabaseMetaData.getDatabaseProductVersion() (~line 417): return JParqVersion.VERSION.
   c. JParqDatabaseMetaData.getDriverVersion() (~line 428): return JParqVersion.VERSION.
   d. JParqDatabaseMetaData.getDriverMajorVersion() (~line 433): return JParqVersion.MAJOR.
   e. JParqDatabaseMetaData.getDriverMinorVersion() (~line 438): return JParqVersion.MINOR.
   f. JParqDriver.getMajorVersion() (~line 62): return JParqVersion.MAJOR.
   g. JParqDriver.getMinorVersion() (~line 68): return JParqVersion.MINOR.
   h. Update JParqDriverTest (~line 45): change assertEquals(0, …) to assertEquals(1, …) for major
      and assertEquals(1, …) to assertEquals(2, …) for minor.
   i. Update JParqDatabaseMetaDataCapabilitiesTest (~line 46): change assertEquals("1.3.0", …) to
      assertEquals(JParqVersion.VERSION, …) (or "1.3.0").
   j. On future version bumps only JParqVersion.java and pom.xml need updating.

5. Medium: improve Parquet index/pruning support observability and coverage. JParq already enables Parquet statistics, column-index, and dictionary filtering when a query can be translated into a
   `FilterPredicate`, but there is no explicit verification of those optimizations, little user-facing documentation about when they apply, and no visibility into when pushdown was used. See
   JParqPreparedStatement.java:364, JParqPreparedStatement.java:473, JParqPreparedStatement.java:1273, JParqPreparedStatement.java:2477, ParquetFilterBuilder.java:49.

   Plan:
   a. Add explicit tests that verify pushdown/pruning is engaged for supported predicates, especially on cases that should benefit from Parquet statistics or column/page indexes.
   b. Document which SQL predicate shapes are pushdownable through `ParquetFilterBuilder` and which ones fall back to residual filtering.
   c. Evaluate bloom-filter support in the Parquet stack used by JParq and wire it in if the dependencies support it in a stable way.
   d. Add debug or trace-level logging and/or lightweight metrics that make it clear when a Parquet predicate was built and attached versus when filtering remained residual.

6. Update release.md with fixes 1-5 when those sections are complete on the release branch (keep it brief).
